[build]
requires-python = ">=3.12"

[project]
name = "fx-sentinel-bot"
version = "1.0.0"
description = "Telegram bot for ECB mid-market FX alerts and 7-day scenarios."
readme = "../README.md"
requires-python = ">=3.12"
license = { text = "MIT" }
dependencies = [
  "httpx==0.28.1",
  "python-telegram-bot==21.10",
]
